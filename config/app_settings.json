{
  "_comment_intro": "Main configuration file. Standard JSON doesn't support comments, so we use _comment fields as notes.",

  "input_mode": "typing",
  "_comment_input_mode": "Choose your primary input method. Options: 'typing' (keyboard) OR 'microphone' (Live STT).",

  "display": {
    "height": 720,
    "width": 1280
  },
  "_comment_display": "The resolution of the local 2D Avatar rendering canvas.",

  "playback": {
    "speed_ms": 100,
    "transition_frames": 5
  },
  "_comment_playback": "speed_ms defines the time between keyframes (33ms ~ 30FPS). transition_frames controls how many smooth frames are injected between two distinct signs.",

  "network": {
    "enable_websocket": true,
    "ws_host": "localhost",
    "ws_port": 8765
  },
  "_comment_network": "Settings to pass 3D coordinate arrays live to an external rendering engine (like Unity). Set 'enable_websocket' to true to start the server.",

  "colors": {
    "f": [0, 255, 255],
    "p": [255, 0, 255],
    "l": [0, 255, 0],
    "r": [0, 255, 0]
  },
  "_comment_colors": "RGB color values for the drawn points. (f=Face, p=Pose, l=Left Hand, r=Right Hand)."
}